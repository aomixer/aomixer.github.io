---
interface Props {
	date: Date;
}

const { date } = Astro.props;
const formatted = new Intl.DateTimeFormat('ja-JP', {
  year: 'numeric',
  month: '2-digit',
  day: '2-digit',
  weekday: 'short',
}).format(date);
---

{formatted}
